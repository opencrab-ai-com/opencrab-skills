# workspace-isolation

`workspace-isolation` is a generalized git-worktree workflow for creating a safe implementation workspace.

## Source

This skill is adapted from a git-worktree workflow and rewritten to remove product-specific references while preserving the safety checks.

## Focus

- worktree location selection
- `.gitignore` verification
- baseline setup and test checks
- safer multi-step development
